using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 게임 사전작업. 로그인, 업데이트 등.
/// </summary>
public class PreWorker : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /// <summary>
    /// 각종 사전작업을 시작.
    /// </summary>
    /// <returns>사전작업 성공 여부</returns>
    public bool PreWork()
    {
        return true;
    }
}
